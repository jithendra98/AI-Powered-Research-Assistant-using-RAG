# main.py
"""
This file previously contained a FastAPI backend.

For Streamlit deployment, the app now runs entirely from `frontend/app.py`
and calls the RAG pipeline directly, so this module is no longer used.
It is kept as a placeholder to avoid import issues if referenced elsewhere.
"""

if __name__ == "__main__":
    print("Use `streamlit run frontend/app.py` to start the app.")
